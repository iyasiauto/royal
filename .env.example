# Royal pipeline credentials. Copy to .env and fill in real values.
# .env is git-ignored — never commit real keys.

# ai33pro TTS (RULE 6 word-level SRT)
VOICEOVER_API_KEY=sk_your_ai33pro_key_here

# Serper image search (RULE 24 entity image fetch)
SERPER_API_KEY=your_serper_key_here

# Optional — Groq Whisper for subtitle re-sync (rarely needed)
SUBTITLE_API_KEY=gsk_your_groq_key_here
